import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic5loginsignupform',
  appName: 'login-signup',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
